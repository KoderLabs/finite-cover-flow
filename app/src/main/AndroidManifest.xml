<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.saeed.infiniteflow.example">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".floatingheader.FloatingHeaderActivity"/>
        <activity android:name=".overlapslider.VerticalOverlapExampleActivity" />
        <activity android:name=".overlapslider.HorizontalOverlapExampleActivity" />
        <activity android:name=".ecommerce.HorizontalEcommorceItemExampleActivity" />
        <activity android:name=".ecommerce.VerticalEcommorceItemExampleActivity" />
        <activity android:name=".simplecrousel.VerticalImageViewerExampleActivity" />
        <activity android:name=".simplecrousel.HorizontalImageViewerExampleActivity" />
        <activity
            android:name=".fullscreenslider.FullScreenActivity"
            android:label="@string/title_activity_full_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>